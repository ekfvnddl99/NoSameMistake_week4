import React, { Component } from 'react';
import Navigator from '../navigators/floating-navigator';

export default class RecordHome extends React.Component {
  render(){
    return (
      <Navigator />
    );
  }
}

